<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Colours come from the Compose theme; this only needs to be a bar-less Material base. -->
    <style name="Theme.GlucoTrack" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
